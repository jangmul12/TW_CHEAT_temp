@CHEAT(ARG)
#DIM CAR
IF FLAG:1674 == 1
	PRINTFORMW ※텍붕이모드에 치트 넣었다가 생겨나는 모든 오류는 치트 사용자 본인에게 있습니다※
ENDIF
CALL 보너스소질판정
SIF TARGET:99 == 0
	TARGET:99 = ARG
	
CAR = TARGET:99
IF CAR > 0
	LOCAL = CFLAG:CAR:생리주기
	SIF ARG == -1
		LOCAL ++
	SELECTCASE LOCAL
	CASE 1 TO 2
		LOCALS = "안전한 날"
	CASE 6
		LOCALS = "위험일 전날"
	CASE 7
		LOCALS = "위험일"
	CASE 8
		LOCALS = "배란"
	CASEELSE
		LOCALS = "평상시"
	ENDSELECT
ENDIF
CLEARLINE LINECOUNT
DRAWLINE
PRINTL
SETCOLOR 103,228,126
PRINTL 【실적】
RESETCOLOR
SIF GETBIT (FLAG:함락보너스취득상황, 5)
	SETCOLOR 0,255,255
PRINTLC [101] - 생리주기가시화
RESETCOLOR

SIF GETBIT (FLAG:안면보너스취득상황, 2)
	SETCOLOR 0,255,255
PRINTLC [102] - 위치짐작
RESETCOLOR
SIF GETBIT (FLAG:팬티보너스취득상황, 2)
	SETCOLOR 0,255,255
PRINTLC [103] - 성욕간파
RESETCOLOR
PRINTL
SIF GETBIT (FLAG:기술보너스취득상황, 4)
	SETCOLOR 0,255,255
PRINTLC [104] - 청소기
RESETCOLOR
SIF GETBIT (FLAG:기술보너스취득상황, 5)
	SETCOLOR 0,255,255
PRINTLC [105] - 간편 요리 도구
RESETCOLOR
SIF GETBIT (FLAG:팬티보너스취득상황, 4)
	SETCOLOR 0,255,255
PRINTLC [106] - 제단
RESETCOLOR
PRINTL
SIF GETBIT (FLAG:팬티보너스취득상황, 8)
	SETCOLOR 0,255,255
PRINTLC [107] - 절륜
RESETCOLOR
SIF GETBIT (FLAG:기술보너스취득상황, 2)
	SETCOLOR 0,255,255
PRINTLC [108] - 달변
RESETCOLOR
SIF GETBIT (FLAG:함락보너스취득상황, 3)
	SETCOLOR 0,255,255
PRINTLC [109] - 수수께끼의매력
RESETCOLOR
PRINTL
SIF GETBIT (FLAG:탄막승부보너스취득상황, 2)
	SETCOLOR 0,255,255
PRINTLC [110] - 그레이즈능숙
RESETCOLOR
SIF GETBIT (FLAG:탄막승부보너스취득상황, 5)
	SETCOLOR 0,255,255
PRINTLC [111] - 익스텐드
RESETCOLOR
SIF GETBIT (FLAG:탄막승부보너스취득상황, 4)
	SETCOLOR 0,255,255
PRINTLC [112] - 영력충전
RESETCOLOR
PRINTL
SIF GETBIT (FLAG:호감도보너스취득상황, 8)
	SETCOLOR 0,255,255
PRINTLC [113] - 붉은 실
RESETCOLOR
SIF GETBIT (FLAG:호감도보너스취득상황, 6)
	SETCOLOR 0,255,255
PRINTLC [114] - 팬티밀수
RESETCOLOR
SIF GETBIT(FLAG:잉태보너스취득상황, 1)
	SETCOLOR 0,255,255
PRINTLC [115] - 만능 잉태
RESETCOLOR
PRINTL
SIF GETBIT(FLAG:안면보너스취득상황, 5)
	SETCOLOR 0,255,255
PRINTLC [116] - 천객만래
RESETCOLOR
SIF GETBIT(FLAG:기능보너스취득상황, 1)
	SETCOLOR 0,255,255
PRINTLC [117] - C둔감무시
RESETCOLOR
SIF GETBIT(FLAG:기능보너스취득상황, 2)
	SETCOLOR 0,255,255
PRINTLC [118] - B둔감무시
RESETCOLOR
PRINTL
SIF GETBIT(FLAG:기능보너스취득상황, 3)
	SETCOLOR 0,255,255
PRINTLC [119] - V둔감무시
RESETCOLOR
SIF GETBIT(FLAG:기능보너스취득상황, 4)
	SETCOLOR 0,255,255
PRINTLC [120] - A둔감무시
RESETCOLOR
SIF GETBIT(FLAG:기능보너스취득상황, 5)
	SETCOLOR 0,255,255
PRINTLC [121] - 매혹
RESETCOLOR
PRINTL
PRINTL
SETCOLOR 103,228,126
PRINTL 【마스터】
RESETCOLOR
PRINTLC [201] - 마스터 에디트
SIF TALENT:MASTER:농후정액 == 1
	SETCOLOR 0,255,255
PRINTLC [202] - 농후정액
RESETCOLOR
PRINTFORMLC [203] - TSP 변경({MAXBASE:MASTER:TSP})
PRINTL
PRINTFORMLC [204] - 체력 변경({MAXBASE:MASTER:체력})
PRINTFORMLC [205] - 기력 변경({MAXBASE:MASTER:기력})
PRINTFORMLC [206] - 정력 변경({MAXBASE:MASTER:정력})
PRINTL
PRINTLC [207] - 마스터 회복
PRINTFORMLC [208] - 크기변경(%CHEAT_DICK_NAME(TALENT:MASTER:형상)%)
PRINTL
PRINTL
SETCOLOR 103,228,126
PRINTL 【캐릭터】
RESETCOLOR
PRINTLC [301] - 캐릭터 에디터
PRINTLC [302] - 성경험 재설정
PRINTLC [303] - 욕구불만도 일괄 설정
PRINTL
PRINTLC [304] - 체력 일괄 추가
PRINTLC [305] - 기력 일괄 추가
PRINTLC [306] - 전 캐릭터 회복
PRINTL
PRINTLC [307] - 아이가 있는 캐릭터
PRINTL
PRINTL
IF CAR > 0
	SETCOLOR 103,228,126
	PRINTFORML 【대상 캐릭터 : %CALLNAME:CAR%】
	RESETCOLOR
	PRINTFORMLC [401] - 대상 선택
	PRINTFORMLC [402] - 에디트
	PRINTFORMLC [403] - 상성
	PRINTL
	PRINTFORMLC [404] - 성기술
	PRINTFORMLC [405] - 각인
	PRINTFORMLC [406] - 회복(체력/기력)
	PRINTL
	PRINTFORMLC [407] - 욕구불만도({CFLAG:CAR:욕구불만도})
	PRINTFORMLC [408] - 팬티 입수
	PRINTFORMLC [409] - 팬티 변경(%PANTSNAME(EQUIP:CAR:하반신속옷2, CAR)%)
	PRINTL
	SIF TCVAR:CAR:발정
		SETCOLOR 0,255,255
	PRINTLC [410] - 발정 중
	RESETCOLOR
	PRINTFORMLC [411] - 생리주기 변경(%LOCALS%)△
	PRINTL
	IF CFLAG:CAR:수면 && CAR == TARGET
		IF FLAG:994 == 0
			PRINTFORMLC [412] - 수면심도 고정(X)
		ELSE
			SETCOLOR 0,255,255
			PRINTFORMLC [412] - 수면심도 고정(O)
			RESETCOLOR
		ENDIF
		SELECTCASE TCVAR:TARGET:수면심도
			CASE 0 TO 500
				PRINTFORMLC [413] - 당장이라도 일어날 것 같다({TCVAR:CAR:수면심도})
			CASE 501 TO 1500
				PRINTFORMLC [413] - 잘 수 없는 듯이 하고 있다({TCVAR:CAR:수면심도})
			CASE 1501 TO 3000
				PRINTFORMLC [413] - 자고 있다({TCVAR:CAR:수면심도})
			CASE 3001 TO 5000
				PRINTFORMLC [413] - 새근새근 자고 있다({TCVAR:CAR:수면심도})
			CASE IS > 5000
				PRINTFORMLC [413] - 푹 잠들어 있다({TCVAR:CAR:수면심도})
		ENDSELECT
	ENDIF
	SIF CFLAG:CAR:약점잡음 == 1
		SETCOLOR 0,255,255
	PRINTFORMLC [414] - 약점잡음
	RESETCOLOR
	SIF CFLAG:CAR:약점잡힘 == 1
		SETCOLOR 0,255,255
	PRINTFORMLC [415] - 약점잡힘
	RESETCOLOR
	SIF CFLAG:CAR:생해금 == 1
		SETCOLOR 0,255,255
	PRINTFORMLC [416] - 생해금
	RESETCOLOR
ELSE
	SETCOLOR 103,228,126
	PRINTFORML 【대상 캐릭터 : 없음】
	RESETCOLOR
	PRINTFORMLC [401] - 대상 선택
ENDIF
PRINTL
PRINTL
SETCOLOR 103,228,126
PRINTL 【소지품】
RESETCOLOR
PRINTLC [501] - 스키마 10개 보충
SIF FLAG:997 > 0
	SETCOLOR 0,255,255
PRINTFORMLC [502] - 스키마 유지({ITEM:콜렉션의스키마}개)
RESETCOLOR
PRINTFORMLC [503] - 소지금＋(\\ {MONEY})
PRINTL
PRINTFORMLC [504] - 카리스마＋({MONEY:2}개)
PRINTLC [505] - 팬티 1종류씩 입수
PRINTLC [506] - 소지 아이템 변경
PRINTL
PRINTFORM [507] - 미라다의 돌({ITEM:미라다의돌}개)
PRINT  
PRINTBUTTON "[－]", 5071
PRINTBUTTON "[＋]", 5072
PRINTL
PRINTL
SETCOLOR 103,228,126
PRINTL 【시스템】
RESETCOLOR
IF TIME/60 < 10 && TIME%60 < 10
	PRINTFORMLC [601] - 시간(0{TIME/60}:0{TIME%60})
ELSEIF TIME/60 > 9 && TIME%60 < 10
	PRINTFORMLC [601] - 시간({TIME/60}:0{TIME%60})
ELSEIF TIME/60 < 10 && TIME%60 > 9
	PRINTFORMLC [601] - 시간(0{TIME/60}:{TIME%60})
ELSE
	PRINTFORMLC [601] - 시간({TIME/60}:{TIME%60})
ENDIF
IF FLAG:이상기상 == 1
	PRINTLC [602] - 기후[C민감]
ELSEIF FLAG:이상기상 == 2
	PRINTLC [602] - 기후[V민감]
ELSEIF FLAG:이상기상 == 3
	PRINTLC [602] - 기후[A민감]
ELSEIF FLAG:이상기상 == 4
	PRINTLC [602] - 기후[B민감]
ELSEIF FLAG:이상기상 == 5
	PRINTLC [602] - 기후[M민감]
ELSEIF FLAG:이상기상 == 6
	PRINTLC [602] - 기후[치마]
ELSEIF FLAG:이상기상 == 7
	PRINTLC [602] - 기후[팬티]
ELSEIF FLAG:이상기상 == 8
	PRINTLC [602] - 기후[분홍바람]
ELSEIF FLAG:이상기상 == 9
	PRINTLC [602] - 기후[태풍]
ELSEIF FLAG:이상기상 == 10
	PRINTLC [602] - 기후[비바람]
ELSE
	PRINTLC [602] - 기후
ENDIF
IF FLAG:991 == 0
	PRINTFORMLC [603] - 시간이 흐른다
ELSE
	SETCOLOR 0,255,255
	PRINTFORMLC [603] - 시간이 흐르지 않는다
	RESETCOLOR
ENDIF
PRINTL
IF FLAG:992 == 0
	PRINTFORMLC [604] - 당신은 에너자이저(체력/기력/정력 최대치로 유지)[X]
ELSE
	SETCOLOR 0,255,255
	PRINTFORMLC [604] - 당신은 에너자이저(체력/기력/정력 최대치로 유지)[O]
	RESETCOLOR
ENDIF
PRINTL
IF FLAG:993 == 0
	PRINTFORMLC [605] - 동일위치에 있는 캐릭터의 반감/불쾌/억울 제거[X]
ELSE
	SETCOLOR 0,255,255
	PRINTFORMLC [605] - 동일위치에 있는 캐릭터의 반감/불쾌/억울 제거[O]
	RESETCOLOR
ENDIF
PRINTL
IF FLAG:995 == 0
	PRINTFORMLC [606] - 동일위치에 있는 캐릭터의 체력/기력을 최대치로 유지[X]
ELSE
	SETCOLOR 0,255,255
	PRINTFORMLC [606] - 동일위치에 있는 캐릭터의 체력/기력을 최대치로 유지[O]
	RESETCOLOR
ENDIF
PRINTL
IF FLAG:996 == 0
	PRINTLC [607] - 당신의 버프(인챈트/가호/풍요)를 유지(X)
ELSEIF FLAG:996 == 1
	SETCOLOR 0,255,255
	PRINTLC [607] - 당신의 버프(인챈트/가호/풍요)를 유지(O)
	RESETCOLOR
ENDIF
RESETCOLOR
PRINTL
PRINTL
PRINTLC [999] - 돌아간다
PRINTL
INPUT
CLEARLINE 1
PRINTL
IF RESULT == 999
	RETURN 1
ELSEIF RESULT == 101
	IF !GETBIT (FLAG:함락보너스취득상황, 5)
		SETBIT FLAG:함락보너스취득상황, 5
	ELSE
		CLEARBIT FLAG:함락보너스취득상황, 5
	ENDIF
ELSEIF RESULT == 102
	IF !GETBIT (FLAG:안면보너스취득상황, 2)
		SETBIT FLAG:안면보너스취득상황, 2
	ELSE
		CLEARBIT FLAG:안면보너스취득상황, 2
	ENDIF
ELSEIF RESULT == 103
	IF !GETBIT (FLAG:팬티보너스취득상황, 2)
		SETBIT FLAG:팬티보너스취득상황, 2
	ELSE
		CLEARBIT FLAG:팬티보너스취득상황, 2
	ENDIF
ELSEIF RESULT == 104
	IF !GETBIT (FLAG:기술보너스취득상황, 4)
		SETBIT FLAG:기술보너스취득상황, 4
	ELSE
		CLEARBIT FLAG:기술보너스취득상황, 4
	ENDIF
ELSEIF RESULT == 105
	IF !GETBIT (FLAG:기술보너스취득상황, 5)
		SETBIT FLAG:기술보너스취득상황, 5
	ELSE
		CLEARBIT FLAG:기술보너스취득상황, 5
	ENDIF
ELSEIF RESULT == 106
	IF !GETBIT (FLAG:팬티보너스취득상황, 4)
		SETBIT FLAG:팬티보너스취득상황, 4
	ELSE
		CLEARBIT FLAG:팬티보너스취득상황, 4
	ENDIF
ELSEIF RESULT == 107
	IF !GETBIT (FLAG:팬티보너스취득상황, 8)
		SETBIT FLAG:팬티보너스취득상황, 8
	ELSE
		CLEARBIT FLAG:팬티보너스취득상황, 8
	ENDIF
ELSEIF RESULT == 108
	IF !GETBIT (FLAG:기술보너스취득상황, 2)
		SETBIT FLAG:기술보너스취득상황, 2
	ELSE
		CLEARBIT FLAG:기술보너스취득상황, 2
	ENDIF
ELSEIF RESULT == 109
	IF !GETBIT (FLAG:함락보너스취득상황, 3)
		SETBIT FLAG:함락보너스취득상황, 3
	ELSE
		CLEARBIT FLAG:함락보너스취득상황, 3
	ENDIF
ELSEIF RESULT == 110
	IF !GETBIT (FLAG:탄막승부보너스취득상황, 2)
		SETBIT FLAG:탄막승부보너스취득상황, 2
	ELSE
		CLEARBIT FLAG:탄막승부보너스취득상황, 2
	ENDIF
ELSEIF RESULT == 111
	IF !GETBIT (FLAG:탄막승부보너스취득상황, 5)
		SETBIT FLAG:탄막승부보너스취득상황, 5
	ELSE
		CLEARBIT FLAG:탄막승부보너스취득상황, 5
	ENDIF
ELSEIF RESULT == 112
	IF !GETBIT (FLAG:탄막승부보너스취득상황, 4)
		SETBIT FLAG:탄막승부보너스취득상황, 4
	ELSE
		CLEARBIT FLAG:탄막승부보너스취득상황, 4
	ENDIF
ELSEIF RESULT == 113
	IF !GETBIT (FLAG:호감도보너스취득상황, 8)
		SETBIT FLAG:호감도보너스취득상황, 8
	ELSE
		CLEARBIT FLAG:호감도보너스취득상황, 8
	ENDIF
ELSEIF RESULT == 114
	IF !GETBIT (FLAG:호감도보너스취득상황, 6)
		SETBIT FLAG:호감도보너스취득상황, 6
	ELSE
		CLEARBIT FLAG:호감도보너스취득상황, 6
	ENDIF
ELSEIF RESULT == 115
	IF !GETBIT (FLAG:잉태보너스취득상황, 1)
		SETBIT FLAG:잉태보너스취득상황, 1
	ELSE
		CLEARBIT FLAG:잉태보너스취득상황, 1
	ENDIF
ELSEIF RESULT == 116
	IF !GETBIT (FLAG:안면보너스취득상황, 5)
		SETBIT FLAG:안면보너스취득상황, 5
	ELSE
		CLEARBIT FLAG:안면보너스취득상황, 5
	ENDIF
ELSEIF RESULT == 117
	IF !GETBIT (FLAG:기능보너스취득상황, 1)
		SETBIT FLAG:기능보너스취득상황, 1
	ELSE
		CLEARBIT FLAG:기능보너스취득상황, 1
	ENDIF
ELSEIF RESULT == 118
	IF !GETBIT (FLAG:기능보너스취득상황, 2)
		SETBIT FLAG:기능보너스취득상황, 2
	ELSE
		CLEARBIT FLAG:기능보너스취득상황, 2
	ENDIF
ELSEIF RESULT == 119
	IF !GETBIT (FLAG:기능보너스취득상황, 3)
		SETBIT FLAG:기능보너스취득상황, 3
	ELSE
		CLEARBIT FLAG:기능보너스취득상황, 3
	ENDIF
ELSEIF RESULT == 120
	IF !GETBIT (FLAG:기능보너스취득상황, 4)
		SETBIT FLAG:기능보너스취득상황, 4
	ELSE
		CLEARBIT FLAG:기능보너스취득상황, 4
	ENDIF
ELSEIF RESULT == 121
	IF !GETBIT (FLAG:기능보너스취득상황, 5)
		SETBIT FLAG:기능보너스취득상황, 5
	ELSE
		CLEARBIT FLAG:기능보너스취득상황, 5
	ENDIF
ELSEIF RESULT == 201
	CALL CHAR_EDITOR(MASTER,2)
ELSEIF RESULT == 202
	IF TALENT:MASTER:농후정액
		TALENT:MASTER:농후정액 = 0
	ELSE
		TALENT:MASTER:농후정액 = 1
	ENDIF
ELSEIF RESULT == 203
	CALL CHEAT_MODE_203
ELSEIF RESULT == 204
	PRINTFORML 변경할 체력을 입력하세요.
	INPUT
	MAXBASE:MASTER:체력 = RESULT
	BASE:MASTER:체력 = MAXBASE:MASTER:체력
	PRINTFORML 체력이 {MAXBASE:MASTER:체력}이 되었습니다.
ELSEIF RESULT == 205
	PRINTFORML 변경할 기력을 입력하세요.
	INPUT
	MAXBASE:MASTER:기력 = RESULT
	BASE:MASTER:기력 = MAXBASE:MASTER:기력
	PRINTFORML 기력이 {MAXBASE:MASTER:기력}이 되었습니다.
ELSEIF RESULT == 206
	PRINTFORML 변경할 정력을 입력하세요.
	INPUT
	MAXBASE:MASTER:정력 = RESULT
	BASE:MASTER:정력 = MAXBASE:MASTER:정력
	PRINTFORML 정력이 {MAXBASE:MASTER:정력}이 되었습니다.
ELSEIF RESULT == 207
	BASE:MASTER:체력 = MAXBASE:MASTER:체력
	BASE:MASTER:기력 = MAXBASE:MASTER:기력
	BASE:MASTER:정력 = MAXBASE:MASTER:정력
	BASE:MASTER:TSP = MAXBASE:MASTER:TSP
	PRINTW 체력/기력/정력/TSP를 회복하였습니다.
ELSEIF RESULT == 208
	CALL CHEAT_DICK_TYPE(MASTER)
ELSEIF RESULT == 301
	CALL CHEAT_CHAR_EDIT
ELSEIF RESULT == 302
	CLEARLINE LINECOUNT
	CALL VIRGIN_SET
ELSEIF RESULT == 303
	CALL CHEAT_MODE_303
ELSEIF RESULT == 304
	PRINTFORML 추가할 체력을 입력하세요.
	INPUT
	FOR LOCAL, 1, CHARANUM
		MAXBASE:LOCAL:체력 += RESULT
		BASE:LOCAL:체력 = MAXBASE:LOCAL:체력
	NEXT
ELSEIF RESULT == 305
	PRINTFORML 추가할 기력을 입력하세요.
	INPUT
	FOR LOCAL, 1, CHARANUM
		MAXBASE:LOCAL:기력 += RESULT
		BASE:LOCAL:기력 = MAXBASE:LOCAL:기력
	NEXT
ELSEIF RESULT == 306
	FOR LOCAL, 1, CHARANUM
		BASE:LOCAL:체력 = MAXBASE:LOCAL:체력
		BASE:LOCAL:기력 = MAXBASE:LOCAL:기력
	NEXT
	PRINTL 캐릭터의 체력과 기력이 회복되었습니다.
ELSEIF RESULT == 307
	CALL CAEAT_CHAR_CHILD
ELSEIF RESULT == 401
	CALL CHEAT_CHAR_SELECT
ELSEIF RESULT == 402
	CALL CHAR_EDITOR(CAR,2)
ELSEIF RESULT == 403
	CALL CHEAT_RELATIONS(CAR)
ELSEIF RESULT == 404
	CALL CHEAT_SEXTECH(CAR)
ELSEIF RESULT == 405
	CALL CHEAT_MARK(CAR)
ELSEIF RESULT == 406
	BASE:CAR:체력 = MAXBASE:CAR:체력
	BASE:CAR:기력 = MAXBASE:CAR:기력
	PRINTL %CALLNAME:CAR%의 체력과 기력이 회복되었습니다.
ELSEIF RESULT == 407
	CALL CHEAT_MODE_407(CAR)
ELSEIF RESULT == 408
	CALL CHEAT_MODE_408(CAR)
ELSEIF RESULT == 409
	CALL CHEAT_MODE_409(CAR)
ELSEIF RESULT == 410
	IF TCVAR:CAR:발정 != 0
		TCVAR:CAR:발정 = 0
	ELSE
		TCVAR:CAR:발정 = 1
	ENDIF
ELSEIF RESULT == 411
	SELECTCASE CFLAG:CAR:생리주기
	CASE 0 TO 5
		CFLAG:CAR:생리주기 = 6
	CASE 6
		CFLAG:CAR:생리주기 = 7
	CASE 7
		CFLAG:CAR:생리주기 = 8
	CASE 8 TO 10
		CFLAG:CAR:생리주기 = 1
	ENDSELECT
ELSEIF RESULT == 412
	IF FLAG:994 == 0
		FLAG:994 = TCVAR:CAR:수면심도
	ELSEIF FLAG:994 > 0
		FLAG:994 = 0
	ENDIF
ELSEIF RESULT == 413
	CALL CHEAT_MODE_413(CAR)
ELSEIF RESULT == 414
	IF CFLAG:CAR:약점잡음 == 1
		CFLAG:CAR:약점잡음 = 0
	ELSEIF CFLAG:CAR:약점잡음 == 0
		CFLAG:CAR:약점잡음 = 1
	ENDIF
ELSEIF RESULT == 415
	IF CFLAG:CAR:약점잡힘 == 1
		CFLAG:CAR:약점잡힘 = 0
	ELSEIF CFLAG:CAR:약점잡힘 == 0
		CFLAG:CAR:약점잡힘 = 1
	ENDIF
ELSEIF RESULT == 416
	IF CFLAG:CAR:생해금 == 1
		CFLAG:CAR:생해금 = 0
	ELSEIF CFLAG:CAR:생해금 == 0
		CFLAG:CAR:생해금 = 1
	ENDIF
ELSEIF RESULT == 501
	ITEM:콜렉션의스키마 += 10
	PRINTFORMW 콜렉션의 스키마를 10개 보충 되었습니다. (남은 수：{ITEM:콜렉션의스키마})
ELSEIF RESULT == 502
	IF FLAG:997 > 0
		FLAG:997 = 0
	ELSE
		FLAG:997 = ITEM:콜렉션의스키마
	ENDIF
ELSEIF RESULT == 503
	PRINTL
	PRINTFORML 얼마를 추가할까? (현재 보유금 \\{MONEY})
	INPUT
	MONEY += RESULT
ELSEIF RESULT == 504
	PRINTL
	PRINTFORML 얼마를 추가할까? (현재 보유 카리스마 {MONEY:2})
	INPUT
	MONEY:2 += RESULT
ELSEIF RESULT == 505
	CALL CHEAT_MODE_505
ELSEIF RESULT == 506
	CALL CHEAT_ITEM_EDIT
ELSEIF RESULT == 507 || RESULT == 5071 || RESULT == 5072
	IF RESULT == 5071
		SIF ITEM:미라다의돌 > 0
			ITEM:미라다의돌 -= 1
	ELSEIF RESULT == 5072
		ITEM:미라다의돌 += 1
	ELSE
		PRINTL 입수하는 미라다의 돌의 개수를 입력하세요.
		INPUT
		ITEM:미라다의돌 += RESULT
	ENDIF
ELSEIF RESULT == 601
	CALL CHEAT_TIME_CHANGE
ELSEIF RESULT == 602
	CALL CHEAT_SET_WEATHER
ELSEIF RESULT == 603
	IF FLAG:991 == 0
		IF CAR > -1
			TIME:99 = TIME
		ELSE
			TIME:99 = TIME:3
		ENDIF
		FLAG:991 = 1
	ELSEIF FLAG:991 == 1
		TIME:99 = 0
		FLAG:991 = 0
	ENDIF
ELSEIF RESULT == 604
	IF FLAG:992 == 0
		FLAG:992 = 1
	ELSEIF FLAG:992 == 1
		FLAG:992 = 0
	ENDIF
ELSEIF RESULT == 605
	IF FLAG:993 == 0
		FLAG:993 = 1
	ELSEIF FLAG:993 == 1
		FLAG:993 = 0
	ENDIF
ELSEIF RESULT == 606
	IF FLAG:995 == 0
		FLAG:995 = 1
	ELSEIF FLAG:995 == 1
		FLAG:995 = 0
	ENDIF
ELSEIF RESULT == 607
	IF FLAG:996 == 0
		FLAG:996 = 1
	ELSEIF FLAG:996 == 1
		FLAG:996 = 0
	ENDIF
ENDIF
PRINTL
RESTART

@CHEAT_MODE_203
LOCAL = LINECOUNT
DRAWLINE
PRINTFORML 원하는 TSP레벨을 선택 (현재: {MAXBASE:MASTER:TSP})
RESETCOLOR
PRINTBUTTON "[수동입력]", 0
PRINTL
PRINTBUTTON "[ 200 TSP]", 200
PRINTL
PRINTBUTTON "[ 500 TSP]", 500
PRINTL
PRINTBUTTON "[ 800 TSP]", 800
PRINTL
PRINTBUTTON "[1500 TSP]", 1500
PRINTL
PRINTBUTTON "[2500 TSP]", 2500
PRINTL
PRINTBUTTON "[4000 TSP]", 4000
PRINTL
PRINT [999] 돌아가기
PRINTL
INPUT
IF GROUPMATCH(RESULT,100,200,500,800,1500,2500,4000)
	MAXBASE:MASTER:TSP = RESULT
	BASE:MASTER:TSP = RESULT
	PRINTFORMW 현재 TPS {RESULT}
ELSEIF GROUPMATCH(RESULT,0)
	PRINTL 변경할 TSP를 입력하세요.
	INPUT
	MAXBASE:MASTER:TSP = RESULT
	BASE:MASTER:TSP = RESULT
ELSEIF RESULT == 999
	CLEARLINE LINECOUNT - LOCAL
	RETURN
ELSE
	PRINTFORMW 잘못된 입력이다
ENDIF
CLEARLINE LINECOUNT - LOCAL
RESTART

@CHEAT_MODE_303
PRINTL 전 캐릭터의 욕구불만도를 변경합니다.
PRINTL
PRINTBUTTON "[입력]", -1
PRINT 　
PRINTBUTTON "[   0]", 0
PRINT 　
PRINTBUTTON "[ 300]", 300
PRINT 　
PRINTBUTTON "[ 500]", 500
PRINT 　
PRINTBUTTON "[ 700]", 700
PRINT 　
PRINTBUTTON "[1000]", 1000
PRINTL
PRINTL
PRINT [999] 돌아가기
PRINTL
INPUT
IF GROUPMATCH(RESULT,0,300,500,700,1000)
	FOR LOCAL, 1, CHARANUM
		CFLAG:LOCAL:욕구불만도 = RESULT
	NEXT
ELSEIF RESULT == 999
	RETURN
ELSE
	CLEARLINE 1
	PRINTFORML 0 ~ 1000이내의 숫자를 입력해 주세요.(그 외의 숫자를 입력하면 취소됩니다.)
	INPUT
	SIF RESULT < 0 || RESULT > 1000
		RESTART
	FOR LOCAL, 1, CHARANUM
		CFLAG:LOCAL:욕구불만도 = RESULT
	NEXT
ENDIF

@CHEAT_MODE_407(ARG)
PRINTFORML %CALLNAME:ARG%의 욕구불만도를 변경합니다.
PRINTL
PRINTBUTTON "[입력]", -1
PRINT 　
PRINTBUTTON "[   0]", 0
PRINT 　
PRINTBUTTON "[ 300]", 300
PRINT 　
PRINTBUTTON "[ 500]", 500
PRINT 　
PRINTBUTTON "[ 700]", 700
PRINT 　
PRINTBUTTON "[1000]", 1000
PRINTL
PRINTL
PRINT [999] 돌아가기
PRINTL
INPUT
IF GROUPMATCH(RESULT,0,300,500,700,1000)
	CFLAG:ARG:욕구불만도 = RESULT
ELSEIF RESULT == 999
	RETURN
ELSE
	CLEARLINE 1
	PRINTFORML 0 ~ 1000이내의 숫자를 입력해 주세요.(그 외의 숫자를 입력하면 취소됩니다.)
	INPUT
	SIF RESULT < 0 || RESULT > 1000
		RESTART
	CFLAG:LOCAL:욕구불만도 = RESULT
ENDIF

@CHEAT_MODE_408(ARG)
LOCAL:1 = 0
PRINTFORM %CALLNAME:ARG%의 팬티를 획득합니다.
PRINT 　
PRINTBUTTON "[－]", 100
PRINTBUTTON "[＋]", 200
PRINTL
FOR LOCAL, 1, MAXPANTS
	SELECTCASE LOCAL
		CASE 8, 9
			CONTINUE
		CASEELSE
			LOCAL:1 ++
			IF LOCAL:1 < 10
				PRINTFORM %PANTSNAME(LOCAL, ARG), 25, LEFT%[{CFLAG:ARG:(LOCAL + 팬티관리)}장]
			ELSE
				PRINTFORM %PANTSNAME(LOCAL, ARG), 25, LEFT%[{CFLAG:ARG:(LOCAL + 팬티관리)}장]
			ENDIF
			PRINTBUTTON "[－]", LOCAL * 2
			PRINTBUTTON "[＋]", LOCAL * 2 + 1
			PRINT 　
			SIF LOCAL:1 % 3 == 0
				PRINTL
	ENDSELECT
NEXT
PRINTL
PRINT [999] 돌아가기
PRINTL
INPUT
PRINTL
IF RESULT == 999
	RETURN
ELSEIF RESULT == 200
	FOR LOCAL:1, 1, MAXPANTS
		SELECTCASE LOCAL:1
			CASE 8, 9
				CONTINUE
			CASEELSE
				CALL CHEAT_ADD_PANTS(ARG, LOCAL:1)
				CONTINUE
		ENDSELECT
	NEXT
ELSEIF RESULT == 100
	FOR LOCAL:1, 1, MAXPANTS
		SELECTCASE LOCAL:1
			CASE 8, 9
				CONTINUE
			CASEELSE
				CALL CHEAT_SUB_PANTS(ARG, LOCAL:1)
				CONTINUE
		ENDSELECT
	NEXT
ELSE
	LOCAL:1 = RESULT / 2
	LOCAL:2 = RESULT % 2
	IF LOCAL:2 == 1
		CALL CHEAT_ADD_PANTS(ARG, LOCAL:1)
	ELSE
		CALL CHEAT_SUB_PANTS(ARG, LOCAL:1)
	ENDIF
ENDIF
CLEARLINE 13
RESTART

@CHEAT_MODE_409(ARG)
DRAWLINE
LOCAL:1 = 0
IF EQUIP:ARG:하반신속옷2 == 0
	PRINTFORM %조사처리(CALLNAME:ARG,"은")% %PANTSNAME(EQUIP:ARG:하반신속옷2, ARG)%입니다.
ELSE
	PRINTFORM %조사처리(CALLNAME:ARG,"이")% 입고있는 팬티는 %PANTSNAME(EQUIP:ARG:하반신속옷2, ARG)%입니다.
ENDIF
PRINTL
FOR LOCAL, 0, MAXPANTS
	SELECTCASE LOCAL
		CASE 8, 9
			CONTINUE
		CASEELSE
			LOCAL:1 ++
			IF EQUIP:ARG:하반신속옷2 == LOCAL
				SETCOLOR 0,255,255
				PRINTBUTTON @"%PANTSNAME(LOCAL, ARG), 25, LEFT%", LOCAL
				RESETCOLOR
			ELSE
				PRINTBUTTON @"%PANTSNAME(LOCAL, ARG), 25, LEFT%", LOCAL
			ENDIF
			PRINT 　
			SIF LOCAL:1 % 3 == 0
				PRINTL
	ENDSELECT
NEXT
PRINTL
PRINTL
PRINT [999] 돌아가기
PRINTL
INPUT
PRINTL
IF RESULT == 999
	RETURN
ELSE
	EQUIP:ARG:하반신속옷2 = RESULT
ENDIF
CLEARLINE 15
RESTART

@CHEAT_MODE_413(ARG)
LOCAL = LINECOUNT
DRAWLINE
PRINTFORML 원하는 수면심도를 선택 (현재: {TCVAR:ARG:수면심도})
RESETCOLOR
PRINTL
PRINTBUTTON "[수면심도:0500] 당장이라도 일어날 것 같다", 500
PRINTL
PRINTBUTTON "[수면심도:1500] 잘 수 없는 듯이 하고 있다", 1500
PRINTL
PRINTBUTTON "[수면심도:3000] 자고 있다", 3000
PRINTL
PRINTBUTTON "[수면심도:5000] 새근새근 자고 있다", 5000
PRINTL
PRINTBUTTON "[수면심도:9999] 푹 잠들어 있다", 9999
PRINTL
PRINT [999] 돌아가기
PRINTL
INPUT
CLEARLINE 1
IF GROUPMATCH(RESULT,500,1500,3000,5000,9999)
	TCVAR:ARG:수면심도 = RESULT
	FLAG:994 = 0
ELSE
	RETURN
ENDIF

@CHEAT_MODE_505
PRINTL
PRINTFORMW %마스터는% 환상향 소녀들의 팬티를 1종류씩 입수하였다.(섹시팬티추가 설정을 해야 입수할수 있는 팬티가 있습니다.)
FOR LOCAL, 1, CHARANUM-1
	FOR LOCAL:1, 1, MAXPANTS
		SELECTCASE LOCAL:1
			CASE 8, 9
				CONTINUE
			CASE 20, 21, 26
				IF FLAG:섹시팬티추가
					CALL CHEAT_ADD_PANTS(LOCAL, LOCAL:1)
				ENDIF
			CASEELSE
				CALL CHEAT_ADD_PANTS(LOCAL, LOCAL:1)
		ENDSELECT
	NEXT
NEXT

@CHEAT_ADD_PANTS(ARG, ARG:1)
CFLAG:ARG:(ARG:1 + 팬티관리) += 1
SIF CFLAG:ARG:(ARG:1 + 팬티관리)
	FLAG:700 ++
RETURN 0

@CHEAT_SUB_PANTS(ARG, ARG:1)
IF CFLAG:ARG:(ARG:1 + 팬티관리) > 0
	CFLAG:ARG:(ARG:1 + 팬티관리) -= 1
	SIF CFLAG:ARG:(ARG:1 + 팬티관리)
		FLAG:700 --
ENDIF
RETURN 0

@CHEAT_CHAR_EDIT
CLEARLINE LINECOUNT
PRINTL
DRAWLINE
RESETCOLOR
PRINTL
FOR LOCAL:1, 101, CHARANUM + 100
	PRINTBUTTON @" [{LOCAL:1}] %CALLNAME:((LOCAL:1)-100),12,LEFT%", LOCAL:1 - 100
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
PRINTL
PRINTL
PRINTL [999] 뒤로
PRINTL
 
INPUT
IF RESULT >= 1 && RESULT < CHARANUM 
	CALL CHEAT_CHAR_OTHERS(RESULT)
ELSEIF RESULT == 999
	RETURN
ENDIF
RESTART

@CAEAT_CHAR_CHILD
CLEARLINE LINECOUNT
DRAWLINE
PRINTL
PRINTL 아이가 있는 캐릭터
PRINTL
LOCAL:1 = 0
FOR LOCAL, 101, CHARANUM + 100
	SIF !CFLAG:(LOCAL-100):아이인원수
		CONTINUE
	LOCAL:1 ++
	PRINTBUTTON @" [{LOCAL}] %CALLNAME:((LOCAL)-100),12,LEFT%", LOCAL - 100
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
PRINTL
IF LOCAL:1 > 0
	PRINTL
	PRINTL [999] 뒤로
	PRINTL
	INPUT
	IF RESULT == 999
		RETURN
	ELSE
		CALL CHEAT_CHILD_LIST(RESULT)
	ENDIF
ELSE
	PRINTW 아이가 있는 캐릭터가 없습니다.
ENDIF

@CHEAT_CHILD_LIST(ARG)
CLEARLINE LINECOUNT
LOCAL:1 = 0
DRAWLINE
PRINTFORML %CALLNAME:ARG% <\@ TALENT:ARG:모성 == 1 ? 모성 # 사라짐 \@>
DRAWLINE
LOCALS = 육아중인 아이
PRINTFORML %LOCALS, 13, LEFT% : %CHILDNAME:ARG:(TALENT:ARG:육아중)%
LOCALS = 출산경험
PRINTFORML %LOCALS, 13, LEFT% : {EXP:ARG:출산경험}
LOCALS = 출산경과일수
PRINTFORML %LOCALS, 13, LEFT% : {CFLAG:ARG:출산경과일}
LOCALS = 육아인원수
PRINTFORML %LOCALS, 13, LEFT% : {CFLAG:ARG:육아인원수}
LOCALS = 아이인원수
PRINTFORML %LOCALS, 13, LEFT% : {CFLAG:ARG:아이인원수}
DRAWLINE
PRINTL
PRINTFORML %CALLNAME:ARG%의 아이들
PRINTL
LOCALS:1 = 탄생일
LOCALS:2 = 탄생경과일
FOR LOCAL, 1, MIN(11, CFLAG:ARG:아이인원수 + 1)
	LOCAL:1 ++
	IF DAY - CHILD_BIRTHDAY:ARG:LOCAL + 1 > CHILD_자립
		PRINTFORM 제 {LOCAL}자녀：%CHILDNAME:ARG:LOCAL, 15, LEFT%<자립>　== 1 ?  ♀ # ♂== 1 ?  ♀ # ♂
	ELSE
		PRINTFORM 제 {LOCAL}자녀：%CHILDNAME:ARG:LOCAL, 15, LEFT%<육아>　\@ CHILD_SEX:ARG:LOCAL == 1 ?  ♀ # ♂ \@
	ENDIF
	IF LOCAL == CFLAG:ARG:아이인원수
		PRINT 　　　
		PRINTBUTTON "[태어난 적이 없다]", LOCAL
	ENDIF
	PRINTL
	IF CHILD_BIRTHDAY:ARG:LOCAL < 10
		PRINTFORM 　%LOCALS:1, 6, LEFT% :    {CHILD_BIRTHDAY:ARG:LOCAL} 
	ELSEIF CHILD_BIRTHDAY:ARG:LOCAL < 100
		PRINTFORM 　%LOCALS:1, 6, LEFT% :   {CHILD_BIRTHDAY:ARG:LOCAL} 
	ELSEIF CHILD_BIRTHDAY:ARG:LOCAL < 1000
		PRINTFORM 　%LOCALS:1, 6, LEFT% :  {CHILD_BIRTHDAY:ARG:LOCAL} 
	ELSE
		PRINTFORM 　%LOCALS:1, 6, LEFT% : {CHILD_BIRTHDAY:ARG:LOCAL} 
	ENDIF
	PRINTBUTTON "≪", 100 + (LOCAL * 4)
	PRINTBUTTON "[－]", 100 + (LOCAL * 4 + 1)
	PRINTBUTTON "[＋]", 100 + (LOCAL * 4 + 2)
	PRINTBUTTON "≫", 100 + (LOCAL * 4 + 3)
	PRINTFORM 　%LOCALS:2, 10, LEFT% : {DAY - CHILD_BIRTHDAY:ARG:LOCAL + 1}
	PRINTL
NEXT
PRINTL
IF LOCAL:1 > 0
	PRINTLC [999] - 뒤로
	PRINTL
	PRINTL
	INPUT
	IF RESULT == 999
		RETURN
	ELSEIF RESULT < 10
		CALL CHEAT_CHILD_DELETE(ARG, RESULT)
		RESTART
	ELSE
		LOCAL:3 = (RESULT - 100) / 4
		LOCAL:4 = (RESULT - 100) % 4
		IF LOCAL:4 == 0
			IF CHILD_BIRTHDAY:ARG:(LOCAL:3) - 9 > 1
				CHILD_BIRTHDAY:ARG:(LOCAL:3) -= 10
				SIF LOCAL:3 == TALENT:ARG:육아중
					CFLAG:ARG:출산경과일 += 10
			ENDIF
		ELSEIF LOCAL:4 == 1
			IF CHILD_BIRTHDAY:ARG:(LOCAL:3) > 1
				CHILD_BIRTHDAY:ARG:(LOCAL:3) --
				SIF LOCAL:3 == TALENT:ARG:육아중
					CFLAG:ARG:출산경과일 ++
			ENDIF
		ELSEIF LOCAL:4 == 2
			IF DAY > CHILD_BIRTHDAY:ARG:(LOCAL:3)
				CHILD_BIRTHDAY:ARG:(LOCAL:3) ++
				SIF LOCAL:3 == TALENT:ARG:육아중
					CFLAG:ARG:출산경과일 --
			ENDIF
		ELSEIF LOCAL:4 == 3
			IF DAY > CHILD_BIRTHDAY:ARG:(LOCAL:3) + 9
				CHILD_BIRTHDAY:ARG:(LOCAL:3) += 10
				SIF LOCAL:3 == TALENT:ARG:육아중
					CFLAG:ARG:출산경과일 -= 10
			ENDIF
		ENDIF
		RESTART
	ENDIF
ELSE
	PRINTFORMW %CALLNAME:ARG%의 아이들이 더 이상 없습니다.
ENDIF

@CHEAT_CHILD_DELETE(ARG:1, ARG:2)
SIF EXP:(ARG:1):출산경험 > 0
	EXP:(ARG:1):출산경험 -= 1
TALENT:(ARG:1):모성 = 0
TALENT:(ARG:1):육아중 = 0
CFLAG:(ARG:1):출산경과일 = 0
CFLAG:(ARG:1):출산휴가중 = 0
SIF CFLAG:(ARG:1):아이인원수 > 0
	CFLAG:(ARG:1):아이인원수 -= 1
SIF CFLAG:MASTER:아이인원수 > 0
	CFLAG:MASTER:아이인원수 -= 1

CHILDNAME:(ARG:1):(ARG:2) = ""
CHILD_BIRTHDAY:(ARG:1):(ARG:2) = 0
CHILD_SEX:(ARG:1):(ARG:2) = 0
CHILD_WEATHER:(ARG:1):(ARG:2) = 0
CHILD_BIRTHDATE:(ARG:1):(ARG:2) = 0
CHILD_LOVE:(ARG:1):(ARG:2) = 0

IF DAY - CHILD_BIRTHDAY:(ARG:1):(ARG:2) + 1 <= CHILD_자립
	SIF CFLAG:MASTER:육아인원수 > 0
		CFLAG:MASTER:육아인원수 -= 1
	SIF CFLAG:(ARG:1):육아인원수 > 0
		CFLAG:(ARG:1):육아인원수 -= 1
	IF (ARG:2)-1 > 0
		IF DAY - CHILD_BIRTHDAY:(ARG:1):((ARG:2)-1) + 1 <= CHILD_자립
			CFLAG:(ARG:1):출산경과일 = DAY - CHILD_BIRTHDAY:(ARG:1):((ARG:2)-1) + 1
			TALENT:(ARG:1):모성 = 1
			TALENT:(ARG:1):육아중 = (ARG:2)-1
		ENDIF
	ENDIF
ENDIF

@CHEAT_CHAR_SELECT
CLEARLINE 1
PRINTL
DRAWLINE
RESETCOLOR
PRINTL
FOR LOCAL:1, 101, CHARANUM + 100
	PRINTBUTTON @" [{LOCAL:1}] %CALLNAME:((LOCAL:1)-100),12,LEFT%", LOCAL:1 - 100
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
PRINTL
PRINTL
PRINTL [999] 뒤로
PRINTL
 
INPUT
IF RESULT >= 1 && RESULT < CHARANUM
	TARGET:99 = RESULT
	RETURN
ELSEIF RESULT == 999
	RETURN
ENDIF
RESTART

@CHEAT_CHAR_OTHERS(ARG)
CLEARLINE 1
PRINTL
DRAWLINE
RESETCOLOR
PRINTL [0] 당신과의 상성을 변경
PRINTL [1] 캐릭터 에디트
PRINTL [2] 성기술 에디트
PRINTL [3] 각인 에디트
PRINTFORML [4] 욕구불만도({CFLAG:ARG:욕구불만도})
SIF GETBIT(TALENT:ARG:2,1)
	PRINTL [5] 자지 크기 변경
PRINTL [6] 출산
PRINT [999] 뒤로
PRINTL
 
INPUT
IF RESULT == 0
	CALL CHEAT_RELATIONS(ARG)
ELSEIF RESULT == 1
	CALL CHAR_EDITOR(ARG,2)
ELSEIF RESULT == 2
	PRINTL
	CALL CHEAT_SEXTECH(ARG)
ELSEIF RESULT == 3
	CALL CHEAT_MARK(ARG)
ELSEIF RESULT == 4
	PRINTFORML 0 ~ 1000이내의 숫자를 입력해 주세요.(그 외의 숫자를 입력하면 취소됩니다.)
	INPUT
	SIF RESULT < 0 || RESULT > 1000
		RESTART
	CFLAG:ARG:욕구불만도 = RESULT
ELSEIF RESULT == 5
	CALL CHEAT_DICK_TYPE(ARG)
ELSEIF RESULT == 6
	CALL BIRTH(ARG)
ELSEIF RESULT == 999
	RETURN
ENDIF
RESTART
 
@CHEAT_RELATIONS(ARG)
PRINTL
DRAWLINE
PRINTL 무엇을 변경할까?
PRINTFORML [0] 호감도 (현재: {CFLAG:ARG:호감도})
PRINTFORML [1] 신뢰도 (현재: {CFLAG:ARG:신뢰도})
PRINT [999] 뒤로
PRINTL
INPUT
CLEARLINE 1
IF RESULT == 0
	PRINTL 원하는 호감도를 입력 (0 으로 무효)...
	INPUT
	PRINTL [0] 뒤로
	SIF RESULT > 0
		CFLAG:ARG:호감도 = RESULT
ELSEIF RESULT == 1
	PRINTL 원하는 신뢰도를 입력 (0 으로 무효)...
	INPUT
	PRINTL [0] 뒤로
	SIF RESULT > 0
		CFLAG:ARG:신뢰도 = RESULT
ELSEIF RESULT == 999
	RETURN
ENDIF
CLEARLINE 9
RESTART

@CHEAT_SEXTECH(ARG)
CLEARLINE 1
PRINTL
LOCAL:1 = 5
LOCAL:2 = 0
 
DRAWLINE
PRINTFORML %CALLNAME:ARG%의 성기술을 변경
RESETCOLOR
 
FOR LOCAL, 50, 60
	SIF !STRLENS(ABLNAME:LOCAL)
		CONTINUE
	SIF !(TALENT:ARG:2 & 1) && (LOCAL == 52 || LOCAL == 54)
		CONTINUE
	PRINTFORM %ABLNAME:LOCAL,10%:
	CALL PRINT_알파벳평가(ARG,3,LOCAL)
	PRINTFORM  {ABL:ARG:LOCAL,5,LEFT}
	IF ABL:ARG:LOCAL > 0
		PRINTBUTTON "[-]", LOCAL - 22
	ELSE
		PRINT [-]
		RESETCOLOR
	ENDIF
	;IF ABL:ARG:LOCAL <= 2
	IF ABL:ARG:LOCAL <= 6
		PRINTBUTTON "[+]", LOCAL
	ELSE
		
		PRINT [+]
		RESETCOLOR
	ENDIF
	LOCAL:2 ++
	IF LOCAL:2 % 3 == 0
		PRINTL
		LOCAL:1 ++
	ENDIF
NEXT
PRINTL
PRINTL [999] 종료
PRINTL
 
INPUT
IF INRANGE(RESULT,28,33)
	ABL:ARG:(RESULT + 22) --
ELSEIF INRANGE(RESULT,50,55)
	ABL:ARG:(RESULT) ++
ELSEIF RESULT == 999
	DRAWLINE
	RETURN
ENDIF
 
CLEARLINE LOCAL:1
RESTART

@CHEAT_MARK(ARG)
PRINTL
DRAWLINE
 
FOR LOCAL, 0, 6
	SIF !STRLENS(MARKNAME:LOCAL)
		CONTINUE
	PRINTFORM %MARKNAME:LOCAL, 12%:
	IF LOCAL == 3
		PRINTFORM %"×" * (MARK:ARG:LOCAL), 10, LEFT%
		RESETCOLOR
	ELSE
		SETCOLOR 255, 255, 0
		PRINTFORM %"★" * (MARK:ARG:LOCAL), 10, LEFT%
		RESETCOLOR
	ENDIF
   
	IF MARK:ARG:LOCAL > 0
		PRINTBUTTON "[-]", LOCAL
	ELSE
		PRINT [-]
		RESETCOLOR
	ENDIF
	IF MARK:ARG:LOCAL < 3 || (LOCAL == 3 && MARK:ARG:LOCAL < 5)
		PRINTBUTTON "[+]", LOCAL + 10
	ELSE
		PRINT [+]
		RESETCOLOR
	ENDIF
	PRINTL
   
NEXT
 
PRINTL
PRINTFORML [999] 마치기
PRINTL
INPUT
IF RESULT < 10
	SIF MARK:ARG:RESULT > 0
		MARK:ARG:RESULT --
ELSEIF RESULT == 999
	RETURN
ELSE
	SIF MARK:ARG:(RESULT-10) < 3
		MARK:ARG:(RESULT-10) ++
ENDIF
CLEARLINE 12
RESTART

@CHEAT_DICK_TYPE(ARG)
CLEARLINE LINECOUNT
DRAWLINE
;if doesn't have dick - return
IF !GETBIT(TALENT:ARG:2,1)
	PRINTW 자지가 없어서 변경할 수 없다!
	RESETCOLOR
	RETURN
ENDIF
 
PRINTFORML %CALLNAME:ARG%의 자지크기를 변경  (현재: %CHEAT_DICK_NAME(TALENT:ARG:형상)%)
RESETCOLOR
 
FOR LOCAL,1,7
	PRINTFORML [{LOCAL}] %CHEAT_DICK_NAME(LOCAL)%
NEXT
PRINTL
PRINTFORML [999] 돌아가기
INPUT
CLEARLINE 1
IF INRANGE(RESULT,1,6)
	TALENT:ARG:형상 = RESULT
	PRINTFORMW 자지크기가 %CHEAT_DICK_NAME(RESULT)%가 되었다.
ELSEIF RESULT == 999
	RETURN
ENDIF
RESTART
 
@CHEAT_DICK_NAME(ARG)
#FUNCTIONS
SELECTCASE ARG
	CASE 0
		LOCALS = 없음
	CASE 1
		LOCALS = 유아 자지
	CASE 2
		LOCALS = 허접 자지
	CASE 3
		LOCALS = 평범한 자지
	CASE 4
		LOCALS = 반딧 자지
	CASE 5
		LOCALS = 돌기 자지
	CASE 6
		LOCALS = 거근
ENDSELECT
RETURNF LOCALS

@CHAR_EDITOR(ARG, ARG:1 = 1)
CLEARLINE LINECOUNT
PRINTL
DRAWLINE
PRINTFORML 이름：%NAME:ARG%　통칭：%CALLNAME:ARG%
CALL BASE_BAR(ARG, "체력", 28, "초록", "「개행」")
CALL BASE_BAR(ARG, "기력", 28, "파랑", "「개행」")
;능력·성기술표시
CALL PRINT_STATE_ABL(ARG)
PRINTL 
;소질표시
CALL PRINT_STATE_TALENT(ARG)
PRINTL 
;경험표시
CALL PRINT_STATE_EXP(ARG)
PRINTL

DRAWLINE
;최종결정
IF LOCAL:100 == 1000
	DRAWLINE
	PRINTL 이것으로 정말로 좋습니까?
;이름의 설정
ELSEIF LOCAL:100 == 1001
	CALL NAME_CUSTOM(ARG)
	LOCAL:100 = 0
	RESTART
;베이스 설정
ELSEIF LOCAL:100 == 1002
	CALL BASE_CUSTOM_CHEAT(ARG)
;소질 설정
ELSEIF LOCAL:100 == 1003
	CALL TALENT_CUSTOM_CHEAT(ARG, LOCAL:101)
;능력 설정
ELSEIF LOCAL:100 == 1004
	CALL ABL_CUSTOM_CHEAT(ARG)
;경험 설정
ELSEIF LOCAL:100 == 1005
	CALL EXP_CUSTOM_CHEAT(ARG)
;성기술 설정
ELSEIF LOCAL:100 == 1006
	CALL CHEAT_SEXTECH(ARG)
ENDIF

;------------------------------
DRAWLINE
PRINTFORML [1000] - \@(LOCAL:100 == 1000) ?  네 # 결정\@  [1001] - 이름의 변경  [1002] - 체력, 기력의 설정
PRINTL [1003] - 소질의 설정  [1004] - 능력의 설정  [1005] - 경험의 설정 [1006] - 성기술의 설정
INPUT
;최종결정
IF RESULT == 1000 && LOCAL:100 == 1000
	RETURN 0
;설정계 입력
ELSEIF RESULT >= 1000
	CLEARLINE LINECOUNT
	LOCAL:100 = RESULT
	RESTART
ENDIF

;베이스 설정
IF LOCAL:100 == 1002
	IF RESULT == 0 && MAXBASE:ARG:0 > 1000
		MAXBASE:ARG:0 -= 100
	ELSEIF RESULT == 1
		MAXBASE:ARG:0 += 100
	ELSEIF RESULT == 2 && MAXBASE:ARG:1 > 1000
		MAXBASE:ARG:1 -= 100
	ELSEIF RESULT == 3
		MAXBASE:ARG:1 += 100
	ENDIF
	BASE:ARG:0 = MAXBASE:ARG:0
	BASE:ARG:1 = MAXBASE:ARG:1
	RESTART
	
;소질 설정
ELSEIF LOCAL:100 == 1003
	SIF RESULT < 0 || RESULT > 500 || TALENTNAME:RESULT == ""
		RESTART
	;함락 소질 외 (1에서도 2에서도 안돼)
	SIF CUSTOM_선택할수없는소질(RESULT, NO:ARG)
		RESTART
	;성별
	IF RESULT == GETNUM(TALENT, "성별")
		IF TALENT:ARG:RESULT == 3
			TALENT:ARG:RESULT = 1
		ELSE
			TALENT:ARG:RESULT ++
		ENDIF
	;성별기호
	ELSEIF RESULT == GETNUM(TALENT, "성별기호")
		IF TALENT:ARG:RESULT == 3
			TALENT:ARG:RESULT = -1
		ELSE
			TALENT:ARG:RESULT ++
		ENDIF
	;온 오프형의 소질
	ELSEIF TALENTNAME:RESULT == GET_TALENTNAME(RESULT, 1) || RESULT == 1 || RESULT == 6
		TALENT:ARG:RESULT = !TALENT:ARG:RESULT
		;온 오프형으로 별도 추가가 필요한 소질
		;모유체질
		IF RESULT == 149
			IF TALENT:ARG:RESULT
				MAXBASE:ARG:모유 = 2000
			ELSE
				MAXBASE:ARG:모유 = 0
			ENDIF
		ENDIF
	;체형
	ELSEIF RESULT == GETNUM(TALENT, "체형")
		;체형이 「소인(-5)」이라고 「거인(3)」의 경우를 독립 처리
		IF TALENT:ARG:RESULT == -5
			TALENT:ARG:RESULT = -2
		ELSEIF TALENT:ARG:RESULT == 3
			TALENT:ARG:RESULT = -5
		ELSE
			TALENT:ARG:RESULT ++
		ENDIF
	;양적 소질
	ELSE
		;구석까지 가면―1을 건다
		IF STRLENS(GET_TALENTNAME(RESULT, TALENT:ARG:RESULT + 1)) || TALENT:ARG:RESULT == -1
			TALENT:ARG:RESULT ++
		ELSE
			TALENT:ARG:RESULT = -1 * TALENT:ARG:RESULT
		ENDIF
	ENDIF
;능력 설정
ELSEIF LOCAL:100 == 1004
	LOCAL = RESULT / 2
	SIF !STRLENS(ABLNAME:LOCAL)
		RESTART
	;동성애 관계
	IF !(TALENT:ARG:2 & 1) && (LOCAL == 17 || LOCAL == 32)
		RESTART
	ELSEIF TALENT:ARG:2 & 1 && (LOCAL == 18 || LOCAL == 33)
		RESTART
	ENDIF
	;중독 관계
	SIF LOCAL >= 30
		RESTART
	;홀수(+)
	IF RESULT % 2
		IF ABL:ARG:LOCAL >= 200
			RESTART
		ELSE
			ABL:ARG:LOCAL ++
		ENDIF
	;짝수(-)
	ELSE
		IF ABL:ARG:LOCAL <= 0
			RESTART
		ELSE
			ABL:ARG:LOCAL --
		ENDIF
	ENDIF
ELSEIF LOCAL:100 == 1005
	IF RESULT == 998
		FOR LOCAL,0,82
		SIF STRLENS(EXPNAME:LOCAL)
			EXP:ARG:(LOCAL) = 0
		NEXT
		RESTART
	ELSEIF RESULT == 999
		FOR LOCAL,0,82
		SIF STRLENS(EXPNAME:LOCAL)
			EXP:ARG:(LOCAL) = RAND:299
		NEXT
		RESTART
	ELSE
		LOCAL = RESULT
		SIF !STRLENS(EXPNAME:LOCAL)
			RESTART
	;동성애 관계
	IF TALENT:ARG:2 & 1 && LOCAL == 41
		RESTART
	ELSEIF TALENT:ARG:2 & 1 && LOCAL == 42
		RESTART
	ENDIF
	PRINTFORM %EXPNAME:LOCAL%를 입력해 주세요	  
	PRINTBUTTON "[캔슬]",-1
	INPUT
	SIF RESULT < 0
		RESTART
	 EXP:ARG:LOCAL = RESULT
  ENDIF
ENDIF
RESTART

;------------------------------
@EXP_CUSTOM_CHEAT(ARG)
VARSET LOCAL
FOR LOCAL,0,100
	SIF EXPNAME:LOCAL == ""
		CONTINUE
	PRINTFORM   [{LOCAL,2}] - %EXPNAME:LOCAL,16%:{EXP:ARG:LOCAL,5,LEFT} 
	LOCAL:1 ++
	SIF LOCAL:1 % 3 == 0
		PRINTL 
NEXT
 
@BASE_CUSTOM_CHEAT(ARG, ARG:6)
CLEARLINE 1
PRINTL
CALL BASE_BAR(ARG, "체력", 28, "초록")
IF BASE:ARG:0 > 1000
	PRINTBUTTON "[－]", 0
ELSE
	SETCOLOR 0x969696
	PRINT [－]
	RESETCOLOR
ENDIF
IF BASE:ARG:0 < (5000*2) || ARG:6
	PRINTBUTTON "[＋]", 1
ELSE
	SETCOLOR 0x969696
	PRINT [＋]
	RESETCOLOR
ENDIF
PRINTL
CALL BASE_BAR(ARG, "기력", 28, "파랑")
IF BASE:ARG:1 > 1000
	PRINTBUTTON "[－]", 2
ELSE
	SETCOLOR 0x969696
	PRINT [－]
	RESETCOLOR
ENDIF
IF BASE:ARG:1 < (5000*2)
	PRINTBUTTON "[＋]", 3
ELSE
	SETCOLOR 0x969696
	PRINT [＋]
	RESETCOLOR
ENDIF
PRINTL
	   
@TALENT_CUSTOM_CHEAT(ARG,ARG:1, ARG:2)
CLEARLINE 1
PRINTL
VARSET LOCAL
FOR LOCAL, 0, 157
	SETCOLOR 150, 150, 150
	IF CUSTOM_선택할수없는소질(LOCAL, NO:ARG) == 1
		CONTINUE
	ELSEIF CUSTOM_선택할수없는소질(LOCAL, NO:ARG) == 2
		;표시는 시킨다
		SETCOLOR 50, 50, 50
		LOCALS = [{LOCAL, 3}]%TALENTNAME:LOCAL%
		GOTO SKIP
	ENDIF
	SELECTCASE TALENTNAME:LOCAL
	;TALENT.CSV에 등록되지 않았다
	CASE ""
		CONTINUE
	;온 오프형의 소질(TALENTNAME와 소질표시가 일치)
	CASE GET_TALENTNAME(LOCAL, 1)
		SIF TALENT:ARG:LOCAL
			SETCOLOR C_AQUA
		LOCALS = [{LOCAL, 3}]%TALENTNAME:LOCAL%
	CASE "비동정"
		SIF !TALENT:ARG:LOCAL
			SETCOLOR C_AQUA
		LOCALS = [{LOCAL, 3}]동정
	CASE "키스미경험"
		SIF !TALENT:ARG:LOCAL
			SETCOLOR C_AQUA
		LOCALS = [{LOCAL, 3}]%TALENTNAME:LOCAL%
	;양적 소질
	CASEELSE
		LOCALS =  [{LOCAL, 3}]%TALENTNAME:LOCAL%
		IF STRLENS(GET_TALENTNAME(LOCAL, TALENT:ARG:LOCAL))
			LOCALS:1 = 【%GET_TALENTNAME(LOCAL, TALENT:ARG:LOCAL)%】
			SETCOLOR C_AQUA
		ELSEIF TALENTNAME:LOCAL == "성별"
			LOCALS:1 = 【여자】
			SETCOLOR C_AQUA
		ELSE
			LOCALS:1 = 【－】
		ENDIF
		LOCALS += LOCALS:1
	ENDSELECT
	$SKIP
	SIF LOCAL:1 % 3 == 0
		PRINTL 
	IF CUSTOM_선택할수없는소질(LOCAL, NO:ARG) == 2
		PRINTPLAINFORM %LOCALS, 30, LEFT%
	ELSE
		PRINTFORM %LOCALS, 30, LEFT%
	ENDIF
	LOCAL:1 ++
NEXT
RESETCOLOR
PRINTL

@ABL_CUSTOM_CHEAT(ARG, ARG:6)
CLEARLINE 1
PRINTL
LOCAL:1 = 0
FOR LOCAL, 0,50
	SIF !STRLENS(ABLNAME:LOCAL)
		CONTINUE
	;동성애 관계
	IF !(TALENT:ARG:2 & 1) && (LOCAL == 17 || LOCAL == 32)
		CONTINUE
	ELSEIF TALENT:ARG:2 & 1 && (LOCAL == 18 || LOCAL == 33)
		CONTINUE
	ENDIF
	;중독도 없음
	SIF LOCAL >= 30
		CONTINUE
	PRINTFORM %ABLNAME:LOCAL,12%:{ABL:ARG:LOCAL, 5, LEFT} 
	IF ABL:ARG:LOCAL != 0
		PRINTBUTTON "[－]", LOCAL * 2
	ELSE
		SETCOLOR 0x969696
		PRINT [－]
		RESETCOLOR
	ENDIF
	;IF ABL:ARG:LOCAL != 2 || ARG:6
	IF ABL:ARG:LOCAL != 50 || ARG:6
		PRINTBUTTON "[＋]", LOCAL * 2 + 1
	ELSE
		SETCOLOR 0x969696
		PRINT [＋]
		RESETCOLOR
	ENDIF
	LOCAL:1 ++
	SIF LOCAL:1 % 2 == 0
		PRINTL 
NEXT
PRINTL

@CHEAT_TIME_CHANGE
CLEARLINE 1
PRINTL
DO
	PRINTL 
	PRINT 　　　┏━
	PRINTBUTTON " △ ", 60
	PRINT ━
	PRINTBUTTON "△", 10
	PRINTBUTTON "△", 1
	PRINT ━┓　　　
	PRINTL 
	PRINT 　　　┃　
	SIF TIME < 600
		PRINT ０
	PRINTFORM %TOFULL(TOSTR(TIME / 60))%：
	SIF TIME % 60 < 10
		PRINT ０
	PRINTFORM %TOFULL(TOSTR(TIME % 60))%　
	PRINT ┃　　　
	PRINTL 
	PRINT 　　　┗━
	PRINTBUTTON " ▽ ", -60
	PRINT ━
	PRINTBUTTON "▽", -10
	PRINTBUTTON "▽", -1
	PRINT ━┛　　　
	PRINTL 
	PRINTL 
	PRINTL 
	PRINTFORML [5555] - 완료
	INPUT
	IF INRANGE(RESULT, -1360, 1360)
		TIME += RESULT
		IF TIME >= 1440
			TIME -= 1440
		ELSEIF TIME < 0
			TIME += 1440
		ENDIF
		CLEARLINE 8
	ELSEIF RESULT == 5555
		RETURN
	ENDIF
LOOP 1

@CHEAT_SET_WEATHER
CLEARLINE 1
PRINTL
PRINT 현재 기후는
IF FLAG:이상기상 == 1
	PRINT  [C민감]입니다
ELSEIF FLAG:이상기상 == 2
	PRINT  [V민감]입니다
ELSEIF FLAG:이상기상 == 3
	PRINT  [A민감]입니다
ELSEIF FLAG:이상기상 == 4
	PRINT  [B민감]입니다
ELSEIF FLAG:이상기상 == 5
	PRINT  [M민감]입니다
ELSEIF FLAG:이상기상 == 6
	PRINT  [치마]입니다
ELSEIF FLAG:이상기상 == 7
	PRINT  [팬티]입니다
ELSEIF FLAG:이상기상 == 8
	PRINT  [분홍바람]입니다
ELSEIF FLAG:이상기상 == 9
	PRINT  [태풍]입니다
ELSEIF FLAG:이상기상 == 10
	PRINT  [비바람]입니다
ELSE
	PRINT  설정되지 않았습니다.
ENDIF
PRINTL
PRINTL
PRINTBUTTONLC "[100] - C민감",1
PRINTBUTTONLC "[101] - V민감",2
PRINTBUTTONLC "[102] - A민감",3
PRINTBUTTONLC "[103] - B민감",4
PRINTBUTTONLC "[104] - M민감",5
PRINTL
PRINTBUTTONLC "[105] - 치마",6
PRINTBUTTONLC "[106] - 팬티",7
PRINTBUTTONLC "[107] - 분홍바람",8
PRINTBUTTONLC "[108] - 태풍",9
PRINTBUTTONLC "[109] - 비바람",10
PRINTL
PRINTBUTTONLC "[000] - 이상기상 제거",0
PRINTL
PRINTL
PRINTLC [999] - 뒤로
PRINTL
INPUT
IF RESULT == 999
	RETURN
ELSE
	FLAG:이상기상 = RESULT
ENDIF

@CHEAT_ITEM_EDIT
CLEARLINE LINECOUNT
SIF LOCALS == ""
	LOCALS = 조교
DRAWLINE
PRINT 　
SIF LOCALS == "조교"
	SETCOLOR 0,255,255
PRINTBUTTONLC "[010] - 조교",9010
RESETCOLOR
PRINT 　
SIF LOCALS == "일용품"
	SETCOLOR 0,255,255
PRINTBUTTONLC "[020] - 일용품",9020
RESETCOLOR
PRINT 　
SIF LOCALS == "소질"
	SETCOLOR 0,255,255
PRINTBUTTONLC "[030] - 소질",9030
RESETCOLOR
PRINT 　
SIF LOCALS == "체질 변화"
	SETCOLOR 0,255,255
PRINTBUTTONLC "[040] - 체질 변화",9040
RESETCOLOR
PRINT 　
SIF LOCALS == "조합물"
	SETCOLOR 0,255,255
PRINTBUTTONLC "[050] - 조합물",9050
RESETCOLOR
PRINTL
PRINT 　
SIF LOCALS == "탄막"
	SETCOLOR 0,255,255
PRINTBUTTONLC "[060] - 탄막",9060
RESETCOLOR
PRINT 　
SIF LOCALS == "소모품"
	SETCOLOR 0,255,255
PRINTBUTTONLC "[070] - 소모품",9070
RESETCOLOR
PRINT 　
SIF LOCALS == "주류"
	SETCOLOR 0,255,255
PRINTBUTTONLC "[080] - 주류",9080
RESETCOLOR
PRINT 　
SIF LOCALS == "식재료"
	SETCOLOR 0,255,255
PRINTBUTTONLC "[090] - 식재료",9090
RESETCOLOR
PRINT 　
SIF LOCALS == "야채"
	SETCOLOR 0,255,255
PRINTBUTTONLC "[100] - 야채",9100
RESETCOLOR
PRINTL
PRINT 　
SIF LOCALS == "고기"
	SETCOLOR 0,255,255
PRINTBUTTONLC "[110] - 고기",9110
RESETCOLOR
PRINT 　
SIF LOCALS == "초콜릿"
	SETCOLOR 0,255,255
PRINTBUTTONLC "[120] - 초콜릿",9120
RESETCOLOR
PRINT 　
SIF LOCALS == "회복"
	SETCOLOR 0,255,255
PRINTBUTTONLC "[130] - 회복",9130
RESETCOLOR
PRINT 　
SIF LOCALS == "원예용품"
	SETCOLOR 0,255,255
PRINTBUTTONLC "[140] - 원예용품",9140
RESETCOLOR
PRINT 　
SIF LOCALS == "소재"
	SETCOLOR 0,255,255
PRINTBUTTONLC "[150] - 소재",9150
RESETCOLOR
PRINTL
PRINT 　
SIF LOCALS == "의상"
	SETCOLOR 0,255,255
PRINTBUTTONLC "[160] - 의상",9160
RESETCOLOR
PRINT 　
SIF LOCALS == "서적"
	SETCOLOR 0,255,255
PRINTBUTTONLC "[170] - 서적",9170
RESETCOLOR
PRINT 　
RESETCOLOR
IF LOCALS != ""
	PRINTL
	DRAWLINE
	LOCAL:1 = 0
	LOCAL:9 = 0
	FOR LOCAL, 0, 1000
		CALL ITEMDATA(LOCAL, "TYPE:" + LOCALS)
		IF RESULT > 0
;			SIF LOCAL == 68 || LOCAL == 69
;				CONTINUE
			LOCAL:1 ++
			IF ITEM:LOCAL < 10
				PRINTFORM 　%ITEMNAME:LOCAL, 22, LEFT%{ITEM:LOCAL}개
			ELSEIF ITEM:LOCAL < 100
				PRINTFORM 　%ITEMNAME:LOCAL, 21, LEFT%{ITEM:LOCAL}개
			ELSE
				PRINTFORM 　%ITEMNAME:LOCAL, 20, LEFT%{ITEM:LOCAL}개
			ENDIF
			PRINT 　
			PRINTBUTTON "≪", LOCAL * 4
			PRINTBUTTON "[－]", LOCAL * 4 + 1
			PRINTBUTTON "[＋]", LOCAL * 4 + 2
			PRINTBUTTON "≫", LOCAL * 4 + 3
			PRINT 　
			IF LOCAL:1 % 3 == 0
				PRINTL
				LOCAL:9 ++
			ENDIF
		ENDIF
	NEXT
	IF LOCAL:1 % 3 > 0
		PRINTL
		LOCAL:9 ++
	ENDIF
ENDIF
FOR LOCAL, 0, (16 - LOCAL:9)
	PRINTL
NEXT
DRAWLINE
PRINTLC [9999] - 뒤로
PRINTL
INPUT
CLEARLINE 3
IF RESULT == 9999
	RETURN
ELSEIF RESULT < 9000
	LOCAL = RESULT / 4
	LOCAL:1 = RESULT % 4
	IF LOCAL:1 == 3
		SIF ITEM:LOCAL == 99
			RESTART
		SELECTCASE LOCALS
			CASE "조교", "일용품", "체질 변화", "서적", "의상", "소질"
				RESTART
			CASE "원예용품"
				SIF LOCAL == 150
					RESTART
			CASE "특수"
				SIF LOCAL != 400 && ITEM:LOCAL == 10
					RESTART
		ENDSELECT
		ITEM:LOCAL += 10
	ELSEIF LOCAL:1 == 2
		SIF ITEM:LOCAL == 99
			RESTART
		SELECTCASE LOCALS
			CASE "조교", "일용품", "체질 변화", "서적", "의상"
				SIF ITEM:LOCAL == 1
					RESTART
			CASE "소질"
			DRAWLINE
				IF ITEM:LOCAL == 3 && (LOCAL == 63 || LOCAL == 64 || LOCAL == 66 || LOCAL == 67 || LOCAL == 68 || LOCAL == 69)
					RESTART
				ELSEIF ITEM:LOCAL == 1 && LOCAL == 62
					RESTART
				ENDIF
			CASE "원예용품"
				SIF ITEM:LOCAL == 1 && LOCAL == 150
					RESTART
		ENDSELECT
		ITEM:LOCAL ++
	ELSEIF LOCAL:1 == 1
		SIF ITEM:LOCAL == 0
			RESTART
		ITEM:LOCAL --
	ELSEIF LOCAL:1 == 0
		IF ITEM:LOCAL < 10
			ITEM:LOCAL = 0
		ELSE
			ITEM:LOCAL -= 10
		ENDIF
	ENDIF
ELSE
	SIF RESULT == 9010
		LOCALS = 조교
	SIF RESULT == 9020
		LOCALS = 일용품
	SIF RESULT == 9030
		LOCALS = 소질
	SIF RESULT == 9040
		LOCALS = 체질 변화
	SIF RESULT == 9050
		LOCALS = 조합물
	SIF RESULT == 9060
		LOCALS = 탄막
	SIF RESULT == 9070
		LOCALS = 소모품
	SIF RESULT == 9080
		LOCALS = 주류
	SIF RESULT == 9090
		LOCALS = 식재료
	SIF RESULT == 9100
		LOCALS = 야채
	SIF RESULT == 9110
		LOCALS = 고기
	SIF RESULT == 9120
		LOCALS = 초콜릿
	SIF RESULT == 9130
		LOCALS = 회복
	SIF RESULT == 9140
		LOCALS = 원예용품
	SIF RESULT == 9150
		LOCALS = 소재
	SIF RESULT == 9160
		LOCALS = 의상
	SIF RESULT == 9170
		LOCALS = 서적
ENDIF
RESTART

@CHEAT_MODE_CHECK(ARG)
SIF FLAG:991 == 1
	TIME = TIME:99

IF FLAG:992 == 1
	BASE:MASTER:체력 = MAXBASE:MASTER:체력
	BASE:MASTER:기력 = MAXBASE:MASTER:기력
	BASE:MASTER:정력 = MAXBASE:MASTER:정력
ENDIF

FOR LOCAL,1,CHARANUM
	IF CFLAG:LOCAL:현재위치 == CFLAG:MASTER:현재위치
		IF FLAG:993 == 1
			PALAM:LOCAL:반감 = 0
			PALAM:LOCAL:불쾌 = 0
			PALAM:LOCAL:억울 = 0
		ENDIF
		IF FLAG:995 == 1
			BASE:LOCAL:체력 = MAXBASE:LOCAL:체력
			BASE:LOCAL:기력 = MAXBASE:LOCAL:기력
		ENDIF
		CONTINUE
	ENDIF
NEXT

IF FLAG:994 > 0 && ARG > 0
	IF CFLAG:ARG:수면
		TCVAR:ARG:수면심도 = FLAG:994
	ELSE
		FLAG:994 = 0
	ENDIF
ENDIF

IF FLAG:996 == 1
	SIF TCVAR:MASTER:인챈트 == 0
		TCVAR:MASTER:인챈트 = 1
	SIF TCVAR:MASTER:가호 == 0
		TCVAR:MASTER:가호 = 1
	SIF TCVAR:MASTER:풍요 == 0
		TCVAR:MASTER:풍요 = 1
ENDIF

SIF FLAG:997 > 0 && ITEM:콜렉션의스키마 != FLAG:997
	ITEM:콜렉션의스키마 = FLAG:997
